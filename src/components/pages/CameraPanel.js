// web-ui/src/components/pages/CameraPanel.js
import React from "react"

// Camera feed panel (roadmap P2 — MJPEG stream + web feed).
// Placeholder: nav entry is hidden until the panel is built.
// To unveil: set hidden: false for PATHS.camera in components/vars.js.
const CameraPanel = () => (
    <div className="border" style={{ margin: "10px", padding: "10px" }}>
        <h2 style={{ marginTop: 0 }}>Camera Panel</h2>
        <p className="label">Coming soon — P2: MJPEG stream + web feed.</p>
    </div>
)

export default CameraPanel